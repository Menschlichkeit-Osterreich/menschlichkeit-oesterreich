name: CodeQL

on:
  push:
    branches: [main, develop, chore/**, feature/**]
    paths:
      - 'apps/website/**'
      - 'apps/api/**'
      - 'apps/crm/**'
      - 'openclaw-system/**'
      - '.github/workflows/codeql.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'apps/website/**'
      - 'apps/api/**'
      - 'apps/crm/**'
      - 'openclaw-system/**'
      - '.github/workflows/codeql.yml'
  schedule:
    # Nightly CodeQL-Scan (01:00 UTC) – unabhängig von Push-Pfad-Filtern
    - cron: '0 1 * * *'

permissions:
  contents: read
  security-events: write

jobs:
  analyze:
    name: CodeQL Analyse (${{ matrix.language }})
    runs-on: ubuntu-latest
    timeout-minutes: 30

    strategy:
      fail-fast: false
      matrix:
        language: [javascript, python]
        # javascript deckt auch TypeScript ab (selber Analyzer).
        # python deckt apps/api/ und openclaw-system/ ab.
        # php würde autobuild benötigen und erhöht Laufzeit; CodeQL-php ist optional.

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - name: Initialize CodeQL
        uses: github/codeql-action/init@45775bd8235c68ba998cffa5171334d58593da47  # v3.28.1
        with:
          languages: ${{ matrix.language }}

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@45775bd8235c68ba998cffa5171334d58593da47  # v3.28.1
        with:
          category: '/language:${{ matrix.language }}'
